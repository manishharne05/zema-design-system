import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import IconButton from '../../components/IconButton.vue';

<Meta
	title="Components/Forms/IconButton"
	component={ IconButton }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'white',
					'green',
					'teal',
					'turquoise',
					'blue',
					'indigo',
					'violet',
					'pink',
					'red',
					'orange',
					'amber',
					'gray',
					'dark'
				],
			}
		},
		size: {
			control:{
				type: 'select',
				options: [
					'sm',
					'md',
					'lg',
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/
`<rds-icon-button
	size="sm"
	icon="trash-outline"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { RdsIconButton: IconButton },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<rds-icon-button
			v-bind="args"
			@rds-click="logIconButtonClick"
		/>`,
	methods: {
		logIconButtonClick(event) {
			console.info('%con click: ', 'color: #2C70CD;', event);
		},
	},
});

<div align="center" style={{display: 'flex'}}>
    <a href="https://rahulaher.netlify.app">
        <img src="../../../img/logo/glyph-black-colored.svg" alt="Rahul Aher logo" width="50" />
        <img src="../../../img/logo/word-black-colored.svg" alt="Rahul Aher" width="150" />
    </a>
</div>

<br />
<br />

# IconButton

### IconButtons are components that allow the user to perform an action with one tap.
---
<br />

## When to use:
- It is necessary to communicate to the user that they can perform an action in the interface, whether in dialogs, modal windows, forms, cards, etc.


<br />

## When not to use:
- It is necessary to give the option to perform an action as a small detail in the interface. In this case, we recommend using links.

<br />

## Preview

<Canvas>
	<Story
		name="IconButton"
		args={{
			size: 'sm',
			icon: 'trash-outline',
			variant: 'white',
			tooltipText: 'Delete',
			disabled: false,
			feedbackOnClick: true,
			feedbackIcon: 'check-outline',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="IconButton" />
