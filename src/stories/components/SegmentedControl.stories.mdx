import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import SegmentedControl from '../../components/SegmentedControl.vue';
import Icon from '../../components/Icon.vue';

<Meta
	title="Components/Navigation/SegmentedControl"
	component={ SegmentedControl }
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<rds-segmented-control
	:segments="['info-outline', 'copy-outline', 'edit-outline']"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { RdsSegmentedControl: SegmentedControl, RdsIcon: Icon },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<rds-segmented-control
			v-bind="args"
			@click="handleEmit"
		>
		</rds-segmented-control>
	`,
	methods: {
		handleEmit(value, index) { console.info('value: ', value); }
	}
});

<div align="center" style={{display: 'flex'}}>
    <a href="https://rahulaher.netlify.app">
        <img src="../../../img/logo/glyph-black-colored.svg" alt="Rahul Aher logo" width="50" />
        <img src="../../../img/logo/word-black-colored.svg" alt="Rahul Aher" width="150" />
    </a>
</div>

<br />
<br />

# SegmentedControl

### SegmentedControls are components that allow the user to view alternative versions of a feature or interface
---
<br />

## Preview

<Canvas>
	<Story
		name="SegmentedControl1"
		args={{
			segments: ['info-outline', 'copy-outline', 'edit-outline'],
			withIcon: false,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="SegmentedControl1" />

<Canvas>
	<Story
		name="SegmentedControl2"
		args={{
			segments: ['info-outline', 'copy-outline', 'edit-outline'],
			segmentsTooltipText: ['info', 'copy', 'edit'],
			withIcon: true,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="SegmentedControl2" />