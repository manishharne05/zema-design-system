import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import Stepper from '../../components/Stepper.vue';

<Meta
	title="Components/Display/Stepper"
	component={ Stepper }
	argTypes={{
		value: {
			value: {
				type: 'numeric',
				min: 0,
				step: 1,
			},
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<rds-stepper
	:steps="steps"
	:value="4"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { RdsStepper: Stepper },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<rds-stepper
			v-bind="args" 
		/>
	`,
});

<div align="center" style={{display: 'flex'}}>
    <a href="https://rahulaher.netlify.app">
        <img src="../../../img/logo/glyph-black-colored.svg" alt="Rahul Aher logo" width="50" />
        <img src="../../../img/logo/word-black-colored.svg" alt="Rahul Aher" width="150" />
    </a>
</div>

<br />
<br />

# Stepper

### Steppers are components that control and display tasks divided into steps.

---
<br />

## When to use:
- It is necessary to visually show the status and current stage of a stepper.
- There is a modal with several steps.
- There are a series of entries or actions that want to show the progress of filling them, regardless of the date and time of the states.

<br />

## When not to use:
- If you need the chronology (date, time) of the steps. To do this, use the Timeline component.
- The order of steps, events or actions is not relevant.

<br />

## Preview

export const steps = [
    { label: 'Operator Data', completed: true, inProcessing: false, error: false },
    { label: 'Flag 2', completed: false, inProcessing: true, error: false },
    { label: 'Flag 3', completed: false, inProcessing: false, error: true },
    { label: 'Flag 4', completed: false, inProcessing: false, error: false },
    { label: 'Flag 5', completed: false, inProcessing: false, error: false },
];

<Canvas>
	<Story
		name="Stepper"
		args={{
			steps,
			value: 4,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Stepper" />